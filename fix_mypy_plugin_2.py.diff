<<<<<<< SEARCH
        class TypeVisitor:
            def __init__(self, plugin, type_map):
                self.plugin = plugin
                self.type_map = type_map
                self.visited = set()

            def visit(self, node):
=======
        class TypeVisitor:
            def __init__(self, plugin, type_map):
                self.plugin = plugin
                self.type_map = type_map
                self.visited = set()

            def visit(self, node):
                if node is None:
                    return
>>>>>>> REPLACE
